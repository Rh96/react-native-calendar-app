/**
 * Storage Keys
 * Constants for secure storage service keys
 */
export const STORAGE_KEYS = {
  CREDENTIALS: 'com.calendarapp.credentials',
  BIOMETRICS_ENABLED: 'com.calendarapp.biometrics_enabled',
} as const;

